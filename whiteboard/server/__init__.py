"""The FastAPI server (T7): ``create_app`` / ``run_foreground`` in ``app``, the websocket
``Hub`` and ``ServerContext``/``Bus`` glue, the client protocol models, the message
``handlers``, the HTTP ``api`` routes and the ``ClaudeBridge`` push channel."""
